trackId: toolchain
# Sources added while writing prose for module `toolchain-invariant-testing` (2026-08-26).
#
# Every URL below was fetched with
#   curl -sSL --max-time 30 -w '\n[%{http_code}] %{url_effective}\n' "<url>"
# on 2026-08-26, the FINAL url was checked, and the BODY was read for the specific claim cited.
# A 200 alone was not accepted as evidence.
#
# WHY THIS FILE DEFINES ONLY ONE NEW SOURCE
# ------------------------------------------
# `foundry-invariant-testing` (toolchain.yaml) is registered as
#   https://getfoundry.sh/forge/invariant-testing
# That URL still returns 200, but it 301s to a DIFFERENT PATH:
#   https://www.getfoundry.sh/guides/invariant-testing
# The page moved from /forge/... to /guides/... during the docs migration. Per the verification rule
# (the final URL must keep the path requested), the old id cannot be treated as body-verified, so a
# second id pointing at the resolved canonical path is defined below and is what the three lessons
# actually cite for guide-body claims. `foundry-invariant-testing` is still cited, but only where the
# claim is also carried by the resolved page. toolchain.yaml is owned by another agent and was NOT
# edited; whoever owns it should consider updating that url.
#
# Two more redirects observed on 2026-08-26, recorded so nobody re-derives them:
#   getfoundry.sh/forge/tests/writing-tests  ->  www.getfoundry.sh/forge/testing   (PATH CHANGED)
#     so `foundry-writing-tests` is NOT cited by this module; `foundry-forge-testing`, whose url is
#     already the destination, is cited instead.
#   getfoundry.sh/config/reference/testing   ->  www.getfoundry.sh/config/reference/testing  (path kept)
#
# ONE SOURCE DELIBERATELY NOT CITED
# ---------------------------------
# `patrick-fuzz-bare-minimum` (medium.com) returned HTTP 403 to an automated fetch on 2026-08-26 —
# a Cloudflare bot challenge, not a dead page. Its body could NOT be read this session, so nothing
# in this module is attributed to it, and it does not appear in any lesson's `sources` list. The
# "fuzzing is now table stakes" argument is carried by the canonical docs instead.
#
# IDS REUSED BY REFERENCE (defined elsewhere, deliberately not redefined here, so the linter's
# CONFLICTING-SOURCE / DUP-SOURCE checks stay quiet):
#   toolchain.yaml — foundry-invariant-testing, foundry-testing-config, foundry-forge-testing,
#     forge-test-reference, foundry-inline-test-config, foundry-cheatcode-assume, forge-std-repo,
#     foundry-ci, foundry-issue-3001, foundry-releases, measured-forge-1-7-1,
#     rareskills-invariant-testing, cyfrin-fuzz-guide, threesigma-invariant, solidity-0836-release
#   solidity.yaml — eip-4626, oz-erc4626-docs, oz-erc4626-inflation-defense
#
# MEASUREMENTS
# ------------
# Every campaign figure quoted in these lessons (128,000 calls / 128,000 reverts with no handler;
# 1,182 self-inflicted `bound(x,1,0)` reverts and the 6.94s -> 27.01s slowdown once they were fixed;
# 10,000 runs at depth 1 passing while 10 runs at depth 500 failed; the 14,662 - 4,662 = 10,000
# discard arithmetic; `[invariant] show_metrics = true` and `depth = 500` in the default profile;
# `--junit` being rejected alongside `--gas-report`; `--no-isolate` not existing) was produced on
# THIS machine on 2026-08-26 with forge 1.7.1 (commit 4072e487, built 2026-05-08) and solc 0.8.36,
# and is attributed to `measured-forge-1-7-1`. The probe project and the exact command lines are
# reproduced inside the lessons themselves.
sources:
  - id: foundry-invariant-guide
    tier: canonical-docs
    title: "Foundry — Invariant testing guide (targetContract, targetSelector, handler pattern, ghost variables, afterInvariant, check_interval, optimization mode)"
    url: https://www.getfoundry.sh/guides/invariant-testing
    publishedAt: "2026-08"
    retrievedAt: "2026-08-26"
    verifiedAt: "2026-08-26"
    vendor: false
