# A Nargo workspace, not a package: a package here would need `src/main.nr` at the repo root, and
# `src/` already belongs to Foundry. Members keep the circuits in their own directory.
[workspace]
members = ["circuits/hint-forgery"]
