# The witness. `nargo check` regenerates this from main's signature, so if you change the
# parameters, delete it and run `nargo check` again.
#
# This is where the forgery happens: the whole practice is finding a value here that the unsound
# circuit accepts and the fixed one does not.
x = 0
