{
  "name": "chainpath-practice-repo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The working copy Chainpath points at. Every exercise here is unsolved on purpose.",
  "scripts": {
    "test": "vitest run",
    "test:junit": "vitest run --reporter=junit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@types/node": "^24.10.0",
    "markdownlint-cli": "^0.45.0",
    "markdownlint-cli2": "^0.19.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
