//! CHAINPATH-GENERATED-SCAFFOLD
//!
//!
//! Practice: protocol-contributing-first-issue  (fix, grain block, difficulty 3)
//! Run:      cargo test --workspace --no-fail-fast 2>&1 | tee out/build-log.txt
//!
//! THIS IS A CHECKLIST, NOT A SPECIFICATION. It was generated so that the command above
//! resolves and fails honestly, with one named case per acceptance criterion, instead of
//! reporting "the test path may not exist" — which is a broken harness, not a red test.
//!
//! Replace each placeholder with a real assertion as you go. A criterion you have actually
//! tested no longer needs its placeholder. Delete this notice when none remain.
//!
//! What the practice asks for:
//!   Clone one execution client and one consensus client whose languages you can actually work
//!   in, get both building from source and their test suites passing locally, and keep a friction
//!   log while you do it: every step that failed, every undocumented prerequisite, how long each
//!   build took, and what you had to search for. That log is a deliverable, not a side effect —
//!   it is the raw material for a documentation contribution. Then pick one issue from either
//!   repository's newcomer-labelled list, confirm on the issue that it is still wanted, and open
//!   a pull request. If the issue turns out to be stale or already fixed, say so on the issue and
//!   pick another — that comment is itself a contribution.

/// Both repositories build from source locally and their default test suites run, with the
/// command output captured
#[test]
fn criterion_01_both_repositories_build_from_source_locally_and() {
    panic!("Both repositories build from source locally and their default test suites run, with the command output captured");
}

/// A friction log records every failure, its cause and its fix, with timings for each build
#[test]
fn criterion_02_a_friction_log_records_every_failure_its() {
    panic!("A friction log records every failure, its cause and its fix, with timings for each build");
}

/// A pull request is open against one of the two repositories, linked from the log, addressing
/// a specific issue
#[test]
fn criterion_03_a_pull_request_is_open_against_one() {
    panic!("A pull request is open against one of the two repositories, linked from the log, addressing a specific issue");
}

/// The write-up names the label taxonomy each repository actually uses, verified in the
/// repository rather than assumed
#[test]
fn criterion_04_the_write_up_names_the_label_taxonomy() {
    panic!("The write-up names the label taxonomy each repository actually uses, verified in the repository rather than assumed");
}
