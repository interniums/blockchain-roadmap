[package]
name = "zkvm-bench"
edition.workspace = true
version.workspace = true
description = "Bench two zkVM provers over the same program and report the difference"

# The acceptance command is `cargo run --release --bin bench`, so the binary is named `bench`
# rather than after the package.
[[bin]]
name = "bench"
path = "src/main.rs"
