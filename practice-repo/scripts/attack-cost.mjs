/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: fundamentals-incentives-price-the-attack-live
 * Run:      node scripts/attack-cost.mjs --source beacon --out results/attack-cost.json
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. Output contains total staked ETH, validator count, the derived one-third figure in ETH, the
     fetch timestamp and the source URL
 *   2. Running without a --eth-price argument prints no fiat figure and exits successfully
 *   3. Running with --eth-price labels the fiat output as derived and echoes the price and
     timestamp used
 *   4. The memo maps at least three distinct trust assumptions of one named application onto the
     three attack thresholds, each with a number
 */

const argv = process.argv.slice(2);
void argv;

console.error('TODO: scripts/attack-cost.mjs is unimplemented (practice fundamentals-incentives-price-the-attack-live)');
process.exit(1);
