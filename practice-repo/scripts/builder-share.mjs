/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: ledgers-finality-measure-builder-share
 * Run:      node scripts/builder-share.mjs --blocks 1000 && node --test test/builder-share.test.mjs
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. The distribution covers 1,000 consecutive blocks and names the top four builders with their
     shares
 *   2. The relay-delivered share of the sample is reported separately from the builder distribution
 *   3. One block is traced from relay bid to on-chain proposer payment, and the payment's location
     in the block is named
 *   4. The output records both published top-two claims alongside the measured figure and states
     which, if either, the measurement supports
 *   5. The sample range and the data source are recorded so the measurement can be repeated
 */

const argv = process.argv.slice(2);
void argv;

console.error('TODO: scripts/builder-share.mjs is unimplemented (practice ledgers-finality-measure-builder-share)');
process.exit(1);
