/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: fundamentals-distributed-classify-three-incidents
 * Run:      npx markdownlint docs/incidents/*.md && node scripts/check-incident-writeup.mjs docs/incidents
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. Three separate write-ups exist, each citing a primary post-mortem or client release note
 *   2. Each names the violated property explicitly and defends the classification against one
     counter-reading
 *   3. At least one incident is classified as neither, with the reasoning given
 *   4. Each write-up states whether the failure was observable from a single RPC endpoint
 */

const argv = process.argv.slice(2);
void argv;

console.error('TODO: scripts/check-incident-writeup.mjs is unimplemented (practice fundamentals-distributed-classify-three-incidents)');
process.exit(1);
