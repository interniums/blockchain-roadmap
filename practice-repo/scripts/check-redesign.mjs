/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: altvm-solana-accounts-redesign-a-mapping
 * Run:      node scripts/check-redesign.mjs docs/solana-redesign.md
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. The checker asserts the document contains a table with one row per account and non-empty
     seeds or "not a PDA" for each
 *   2. The checker asserts every instruction section lists at least one writable account and
     identifies the rent payer
 *   3. The checker asserts the parallelism section names at least one pair that serializes and one
     pair that does not
 */

const argv = process.argv.slice(2);
void argv;

console.error('TODO: scripts/check-redesign.mjs is unimplemented (practice altvm-solana-accounts-redesign-a-mapping)');
process.exit(1);
