/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: infra-indexing-two-indexers-one-answer
 * Run:      pnpm tsx scripts/compare-indexers.ts --window 200000 --assert-identical
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. Both indexers return identical top-100 holder rows, verified by a checksum over the ordered
     result set
 *   2. Wall-clock backfill time is reported for each, with the block window and the data source
     each used
 *   3. The comparison names at least three selection axes and scores both candidates on each, not
     on speed alone
 *   4. No step of either deployment targets The Graph's retired hosted service
 */

export {};

const argv: string[] = process.argv.slice(2);
void argv;

console.error('TODO: scripts/compare-indexers.ts is unimplemented (practice infra-indexing-two-indexers-one-answer)');
process.exit(1);
