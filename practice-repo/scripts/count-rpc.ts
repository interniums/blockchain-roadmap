/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: app-wagmi-count-the-requests
 * Run:      pnpm tsx scripts/count-rpc.ts --report report.json && pnpm vitest run test/count-rpc.test.ts
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. report.json records the request count for the identical-args case and for the varied-args
     case, and the test asserts the first is strictly smaller
 *   2. The report states which fields of the query key changed between the two runs
 *   3. The report records what the five components displayed in the chain-mismatch case and whether
     any error was surfaced
 */

export {};

const argv: string[] = process.argv.slice(2);
void argv;

console.error('TODO: scripts/count-rpc.ts is unimplemented (practice app-wagmi-count-the-requests)');
process.exit(1);
