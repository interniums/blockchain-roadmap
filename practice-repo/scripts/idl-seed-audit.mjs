/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: altvm-anchor-idl-seed-audit
 * Run:      anchor build && node scripts/idl-seed-audit.mjs target/idl
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. The script exits non-zero and names each account for which the program declares seeds but
     the IDL carries no derivable seed description
 *   2. The report states, per under-described account, whether a generated client could derive the
     address at all
 *   3. The report records the exact Anchor version, since the presence of the defect depends on it
 */

const argv = process.argv.slice(2);
void argv;

console.error('TODO: scripts/idl-seed-audit.mjs is unimplemented (practice altvm-anchor-idl-seed-audit)');
process.exit(1);
