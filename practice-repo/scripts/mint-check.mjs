/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: altvm-token-2022-mint-onboarding-checklist
 * Run:      node scripts/mint-check.mjs --doc docs/mint-onboarding.md --mint <MINT_ADDRESS>
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. The script exits non-zero for any enabled extension that does not appear in exactly one
     bucket in the document
 *   2. Run against a plain mint, a fee-bearing mint and a permanent-delegate mint, the script
     prints a different verdict for each
 *   3. The document contains a stated policy for mints whose close authority allows the extension
     set to change after listing
 */

const argv = process.argv.slice(2);
void argv;

console.error('TODO: scripts/mint-check.mjs is unimplemented (practice altvm-token-2022-mint-onboarding-checklist)');
process.exit(1);
