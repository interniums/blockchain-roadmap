/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: app-viem-multicall-vs-loop
 * Run:      pnpm tsx scripts/multicall-benchmark.ts --out results.json && pnpm vitest run test/multicall-benchmark.test.ts
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. results.json contains wall-clock milliseconds and an issued-request count for both
     strategies
 *   2. The test asserts the multicall path issued strictly fewer requests than the sequential path
 *   3. The written explanation states that both strategies execute the same twenty reads on-chain
     and identifies the saving as network round trips
 */

export {};

const argv: string[] = process.argv.slice(2);
void argv;

console.error('TODO: scripts/multicall-benchmark.ts is unimplemented (practice app-viem-multicall-vs-loop)');
process.exit(1);
