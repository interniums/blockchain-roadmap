/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: scaling-op-stack-registry-census
 * Run:      node scripts/registry-census.mjs --repo ./superchain-registry
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. Output is a table of every registry chain with name, governed_by_optimism,
     data_availability_type and gas token
 *   2. Chains that are not superchain_level 2, and chains not using Ethereum blob DA, are
     separately counted
 *   3. One chain is checked line by line against the Standard Rollup Charter, with each item marked
     met or not met from registry data
 *   4. The closing note states what registry membership does and does not attest to
 */

const argv = process.argv.slice(2);
void argv;

console.error('TODO: scripts/registry-census.mjs is unimplemented (practice scaling-op-stack-registry-census)');
process.exit(1);
