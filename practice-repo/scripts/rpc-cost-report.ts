/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: infra-rpc-economics-predict-the-bill
 * Run:      pnpm tsx scripts/rpc-cost-report.ts --blocks 100000 --compare-dashboard
 *
 * This is the tool the exercise asks you to build. It exits non-zero on purpose: an empty
 * script that exited 0 would report success for work nobody has done.
 *
 * What it has to end up doing:
 *   1. The report lists calls per method, a predicted unit total, and the actual dashboard figure,
     with the gap under 10 percent
 *   2. The same call counts are priced under a flat per-request plan and the cheaper family for
     this workload is named
 *   3. The chunk size is stated along with which provider cap set it - block range or result size
 *   4. The unit multipliers used are quoted from the provider's own current table with the date
     they were read, not from memory
 */

export {};

const argv: string[] = process.argv.slice(2);
void argv;

console.error('TODO: scripts/rpc-cost-report.ts is unimplemented (practice infra-rpc-economics-predict-the-bill)');
process.exit(1);
