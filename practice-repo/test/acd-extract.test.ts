/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: protocol-contributing-mine-the-acd-notes  (read, grain block, difficulty 2)
 * Run:      npx vitest run test/acd-extract.test.ts
 *
 * THIS IS A CHECKLIST, NOT A SPECIFICATION. It was generated so that the command above
 * resolves and fails honestly, with one named case per acceptance criterion, instead of
 * reporting "the test path may not exist" — which is a broken harness, not a red test.
 *
 * Replace each placeholder with a real assertion as you go. A criterion you have actually
 * tested should no longer contain a fail() call. Delete this notice when none remain.
 *
 * What the practice asks for:
 *   Read the last three months of execution and consensus All Core Devs notes. Extract, into a
 *   structured file, every item that was flagged as needing help, blocked on implementation, or
 *   deferred for lack of capacity — with the call date, the item, the named person or team, and
 *   the linked issue or pull request if one exists. Then narrow to a shortlist of three
 *   workstreams a new contributor could plausibly join, and for each write what the first week's
 *   work would be. Finally, cross-check your list against what actually shipped or was cut in
 *   the most recent upgrade, and note any item you extracted that has since been resolved — the
 *   staleness rate is information about how fast this record moves.
 */
import { describe, it, expect } from 'vitest';

describe('Mine three months of All Core Devs for work that needs doing', () => {
  // A structured file lists extracted items with call date, description, named owner and a link,
  // and a test asserts no entry is missing a date or a source link
  it('01 — A structured file lists extracted items with call date, description,…', () => {
    expect.fail('A structured file lists extracted items with call date, description, named owner and a link, and a test asserts no entry is missing a date or a source link');
  });

  // A test asserts every linked issue or pull request URL resolves
  it('02 — A test asserts every linked issue or pull request URL resolves', () => {
    expect.fail('A test asserts every linked issue or pull request URL resolves');
  });

  // A shortlist of three joinable workstreams is written, each with a concrete first week of
  // work
  it('03 — A shortlist of three joinable workstreams is written, each with a…', () => {
    expect.fail('A shortlist of three joinable workstreams is written, each with a concrete first week of work');
  });

  // The staleness cross-check reports how many extracted items were already resolved by the time
  // of writing
  it('04 — The staleness cross-check reports how many extracted items were already…', () => {
    expect.fail('The staleness cross-check reports how many extracted items were already resolved by the time of writing');
  });
});
