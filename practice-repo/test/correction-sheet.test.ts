/**
 * CHAINPATH-GENERATED-SCAFFOLD
 *
 * Practice: protocol-statelessness-write-the-correction-sheet  (write, grain block, difficulty 2)
 * Run:      npx vitest run test/correction-sheet.test.ts
 *
 * THIS IS A CHECKLIST, NOT A SPECIFICATION. It was generated so that the command above
 * resolves and fails honestly, with one named case per acceptance criterion, instead of
 * reporting "the test path may not exist" — which is a broken harness, not a red test.
 *
 * Replace each placeholder with a real assertion as you go. A criterion you have actually
 * tested no longer needs its placeholder. Delete this notice when none remain.
 *
 * What the practice asks for:
 *   Find real published teaching material — a course module, a documentation page, a widely
 *   cited article — that presents Verkle trees as Ethereum's upcoming state-tree upgrade. Write
 *   the correction sheet a maintainer could apply. For each claim you correct, produce a
 *   structured entry: the original claim, its status now (cancelled, superseded, never
 *   scheduled, or still true), the replacement claim, and the primary source for the
 *   replacement. Cover the tree itself, the reason for the change, the migration approach, the
 *   status of state expiry, and the existence of a competing route through proving. Every status
 *   must be defensible from a specification or project document, not from press coverage, and
 *   where the sources genuinely do not settle a question your entry must say so rather than pick
 *   a side.
 */
import { describe, it, expect } from 'vitest';

describe('Correct a course that still teaches Verkle', () => {
  // The sheet is machine-readable, with every entry carrying original claim, status, replacement
  // and source URL, and a test asserts no entry is missing a source
  it('01 — The sheet is machine-readable, with every entry carrying original…', () => {
    expect.fail('The sheet is machine-readable, with every entry carrying original claim, status, replacement and source URL, and a test asserts no entry is missing a source');
  });

  // A test asserts every source URL resolves and that at least one entry per claim cites a
  // specification or project document rather than press coverage
  it('02 — A test asserts every source URL resolves and that at least one entry…', () => {
    expect.fail('A test asserts every source URL resolves and that at least one entry per claim cites a specification or project document rather than press coverage');
  });

  // The entry for why Verkle was dropped states the reasons as given in the successor proposal's
  // rationale and discussion, and marks that no single announcement exists
  it('03 — The entry for why Verkle was dropped states the reasons as given in the…', () => {
    expect.fail('The entry for why Verkle was dropped states the reasons as given in the successor proposal\'s rationale and discussion, and marks that no single announcement exists');
  });

  // At least one entry is marked unsettled rather than corrected, with the disagreement between
  // sources described
  it('04 — At least one entry is marked unsettled rather than corrected, with the…', () => {
    expect.fail('At least one entry is marked unsettled rather than corrected, with the disagreement between sources described');
  });
});
